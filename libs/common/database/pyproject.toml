[project]
name = "database-lib"
dynamic = ["version"]
dependencies = [
    "alembic>=1.16.2",
    "asyncpg>=0.30.0",
    "sqlalchemy>=2.0.41",
    "sqlparse>=0.5.3",
]

[tool.setuptools.packages.find]
where = ["."]
